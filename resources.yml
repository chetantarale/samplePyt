resources:
  - resource:
    name: resource1
    type: build
    steps:
      - GET item1
      - GET item2

  - resource:
    name: resource2
    type: build
    steps:
      - GET item3
      - GET item4
